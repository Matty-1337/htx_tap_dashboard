# Supabase Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git (it should be in .gitignore)

# Required: Your Supabase project URL
SUPABASE_URL=

# Required: Your Supabase service role key (has full access, keep secret!)
SUPABASE_SERVICE_ROLE_KEY=

# Optional: Storage bucket name (defaults to "client-data" if not set)
SUPABASE_BUCKET=client-data
